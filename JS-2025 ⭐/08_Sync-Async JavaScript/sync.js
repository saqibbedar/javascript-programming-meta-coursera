// Sync: aek k bad dosra hoga, until the first command is not completed others will not start, only if first work has been done then the second one would start.

// In simple words, code will run one by one, one task is completed then the second would start so it will go on.

// code line by line