// Concurrency and Parallelism

// Concurrency: In javascript sync and async code are being executed together, sync in main-stack while async in side-stack, their processing is going one continuously and this what concurrency called in javascript that async and sync code are being processed at same time in different stacks.

// Parallelism: It mainly focuses on the different processors and their cores

// Throttling: to control any codes number of execution, concept to reduce time and controlling the same nature of code for again and again execution

// Example: mouse scrolling etc.