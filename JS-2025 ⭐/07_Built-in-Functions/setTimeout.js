// setTimeout function: It is built in function which takes two arguments which release a function after a specific time passed in second argument.

// syntax: setTimeout(function, 3000);

setTimeout(()=>{
    console.log("Statement will run after 2 seconds");
}, 3000);