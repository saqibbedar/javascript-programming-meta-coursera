// concat(): this method is used to add a string variable with another variable.

// mainly used to join two strings

let greet = 'Hi' 
let user = 'Saqib'

// console.log(greet + user); // you can use + operator to add OR .concat() method to add two strings together

console.log(greet.concat(user)); // Adding with .concat method


let person = 'Saqib'

// length property - used for length of char

console.log(person.length); // 5

// charAt property - read each individual character at a specific index in a string 

console.log(person.charAt(0)); 

// indexOf - returns the location of the first position that matches a character: 

console.log(person .indexOf('A')); 

// split - Split the sting into a array 

console.log(person.split("s")) 



