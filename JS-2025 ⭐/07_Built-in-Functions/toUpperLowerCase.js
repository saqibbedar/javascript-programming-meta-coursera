// toUpperCase(): method is used to capitalize each character.

let user = "saqib";
var upperCase = user.toUpperCase();
console.log(upperCase);

// toLowerCase(): method is used to lowercase each character.

let user2 = "SAQIB";
let LowerCase = user2.toLowerCase();
console.log(user2.toLowerCase());